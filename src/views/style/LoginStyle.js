export const loginStyle = (theme) => {
    return {
        inputField: {
            ...theme.common.inputField
        },
        errorText: {
            ...theme.common.errorText
        },
    }
}